import { toast } from "sonner";


const getData = async () => {
//   toast.error("hiiiiiiiiiiiiii")
}

const page = async () => {
    const data = await getData()
    return (
        <div>
            Enter
        </div>
    );
}

export default page;