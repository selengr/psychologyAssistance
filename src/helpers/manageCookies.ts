// import Cookies from 'js-cookie';

// type TKeyStorage = 'user' | 'activeUser' | 'theme';

// export const COOKIE_KEY: { [key in TKeyStorage]: TKeyStorage } = {
//   user: 'user',
//   activeUser: 'activeUser',
//   theme: 'theme',
// };

// export const setCookies = (key: TKeyStorage, value: string) => {
//   if (!!window) {
//     Cookies.set(key, value);
//   }
// };

// export const getCookies = (key: TKeyStorage) => {
//   if (!!window) {
//     return Cookies.get(key) ?? null;
//   }
//   return null;
// };
